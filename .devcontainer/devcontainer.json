{
  "tasks": {
    "build": "cd donaldfilimon && pip install -r requirements.txt",
    "test": "cd donaldfilimon && pip install -r requirements.txt && pytest",
    "launch": "cd donaldfilimon && python manage.py runserver"
  }
}